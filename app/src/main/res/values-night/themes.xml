<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Night override: dark Material parent, dark window background, light
         status-bar icons. Picked automatically when the device is in dark mode. -->
    <style name="Theme.MemosApp" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">false</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">false</item>
    </style>

    <style name="Theme.MemosApp.Transparent" parent="android:Theme.Translucent.NoTitleBar" />
</resources>
