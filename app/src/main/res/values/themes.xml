<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Day theme: light window background, dark status-bar icons over our
         transparent status bar. -->
    <style name="Theme.MemosApp" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o_mr1">true</item>
    </style>

    <style name="Theme.MemosApp.Transparent" parent="android:Theme.Translucent.NoTitleBar" />
</resources>
